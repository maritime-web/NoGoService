server.contextPath=/weather

# Do not expose /env, /dump and all other default endpoints, except for help
endpoints.enabled=false
endpoints.health.enabled=true

rounding.coordinates=6
rounding.data=3
